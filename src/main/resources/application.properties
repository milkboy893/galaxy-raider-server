# ===============================
# データベース接続設定 (環境変数がなくても繋がる保険付き)
# ===============================
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d72lhps50q8c738t2rs0-a.oregon-postgres.render.com/galaxy_db_xwki}
spring.datasource.username=${DB_USERNAME:galaxy_user}
spring.datasource.password=${DB_PASSWORD:CKDbZGpj1JRrYVOAa47qgmxiukmep0af}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate 設定
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# 今回のエラーの特効薬：URLが無くても強制的にPostgreSQLと認識させる
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect